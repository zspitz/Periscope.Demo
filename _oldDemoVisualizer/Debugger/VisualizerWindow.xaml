<local:VisualizerWindowBase x:Class="_demoVisualizer.VisualizerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:_demoVisualizer"
        xmlns:util="clr-namespace:ZSpitz.Util.Wpf;assembly=ZSpitz.Util.Wpf"
        MinHeight="200" MinWidth="200">
    
    <util:AutoGrid ColumnCount="2" ChildMargin="0,0,0,6">
        <!--<TextBlock>Window number:</TextBlock>
        <TextBlock Text="{Binding WindowNumber}" />
        <Button Command="{Binding OpenInNewWindow}" Grid.ColumnSpan="2">Open next number in new window</Button>
        <Button Command="{Binding CopyWatchExpression}" CommandParameter="{Binding WatchExpressionFormatString}" Grid.ColumnSpan="2">Copy watch expression</Button>-->
    </util:AutoGrid>
    
    <local:VisualizerWindowBase.OptionsPopup>
        <DockPanel>
            <TextBlock Text="Window number:" />
            <!--<TextBox Text="{Binding WindowNumber}" />-->
        </DockPanel>
    </local:VisualizerWindowBase.OptionsPopup>
    
</local:VisualizerWindowBase>
